# Autonomous Skill Activation & Enterprise Engineering Standards

The agent must ALWAYS autonomously apply all specialized skills across all workflows without requiring the user to invoke slash commands (e.g. /skill-name):

1. **Proactive Security Auditing (security-auditor, owasp-top-10, pi-security-testing)**:
1. **Proactive Security Auditing (`security-auditor`, `owasp-top-10`, `api-security-testing`)**:
   - Automatically audit and protect every API, router, and query against the OWASP Top 10, IDOR, PostgREST/SQL injection, rate limit bypasses, and mass assignment.
   - Enforce server-side authentication, role authorization, and input validation without being asked.

2. **TypeScript Mastery (	ypescript-pro)**:
   - Enforce strict typing, zero unwanted ny, comprehensive generics, and immediate 	sc --noEmit verification.
2. **TypeScript Mastery (`typescript-pro`)**:
   - Enforce strict typing, zero unwanted `any`, comprehensive generics, and immediate `tsc --noEmit` verification.

3. **Clean Code & SOLID Refactoring (code-refactoring-refactor-clean)**:
3. **Clean Code & SOLID Refactoring (`code-refactoring-refactor-clean`)**:
   - Write clean, modular, maintainable, single-responsibility functions and components.

4. **Full-Stack Schema Synchronization (omnispace)**:
4. **Full-Stack Schema Synchronization (`omnispace`)**:
   - Synchronize changes across frontend, backend routers, shared schemas, and database migrations simultaneously.

5. **Pragmatic Lean Architecture (ponytail)**:
   - Avoid over-engineering, eliminate unnecessary dependencies, and prioritize standard libraries and native platform APIs.

6. **Knowledge Graph Exploration (graphify)**:
   - Query the knowledge graph to understand blast radius, dependencies, and architectural structure.
